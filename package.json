{
  "name": "mesh-data-persistence",
  "version": "1.0.0",
  "description": "Profile aggregation API using Genderize, Agify, and Nationalize",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "vercel-build": "npx prisma migrate deploy && npx prisma generate"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@prisma/client": "^5.13.0",
    "axios": "^1.15.0",
    "cors": "^2.8.6",
    "express": "^4.18.2",
    "uuid": "^13.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.0",
    "prisma": "^5.13.0"
  }
}
